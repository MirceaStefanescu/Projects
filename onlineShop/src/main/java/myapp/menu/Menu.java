package myapp.menu;

public interface Menu {
    /*
    start(): This method is responsible for starting or initiating the menu functionality. The
    implementation details of this method will be defined by the class that implements this
    interface.
     */
    void start();

    /*
    printMenuHeader(): This method is responsible for printing the header or title of the menu.
    Again, the specific implementation of this method will be provided by the class that
    implements the Menu interface.
     */
    void printMenuHeader();
}
